import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  // Oculta o indicador de desenvolvimento (botão com id="next-logo")
  devIndicators: false,
};

export default nextConfig;
